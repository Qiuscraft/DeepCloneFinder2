import os


# Extract Functions
ef_dataset_path = "~/BigCloneEval/ijadataset/bcb_reduced" # The Path of The Dataset
ef_read_max_workers = 32 # 并发读取文件的最大工作线程数
ef_parse_max_workers = os.cpu_count() # 并发解析文件的最大工作进程数，默认为 CPU 核心数
ef_cache_filepath = f"./cache/function_info/{os.path.basename(os.path.expanduser(ef_dataset_path))}.pkl" # 提取结果的缓存文件路径

# Embedding
emb_batch_size = 32 # 生成embedding时的批处理大小
